# Architecture

Architecture and data-flow diagrams for the platform, expanding on the
flowchart in spec §3. Populated starting Phase 3 (AWS dev foundation) and
updated as each subsequent phase adds a component.
