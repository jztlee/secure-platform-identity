# Terraform module guide

Module guide and HCP Terraform workspace map (spec §5, §17): what each
module under `terraform/modules/` does, its inputs/outputs, and which
workspace applies it. Populated starting Phase 2 (HCP Terraform bootstrap).
