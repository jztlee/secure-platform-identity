# Incident runbooks

Runbooks for: credential leak, compromised workload, failed rollout,
unavailable IdP (spec §14). Populated in Phase 9, alongside the failure
testing that validates each runbook actually works.
