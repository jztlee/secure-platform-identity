apiVersion: v1
kind: ResourceQuota
metadata:
  name: kyverno-quota
  namespace: kyverno
spec:
  hard:
    pods: "15"
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi